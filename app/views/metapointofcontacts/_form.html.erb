<%= form_with(model: metapointofcontact, local: true) do |form| %>
  <% if metapointofcontact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(metapointofcontact.errors.count, "error") %> prohibited this metapointofcontact from being saved:</h2>

      <ul>
      <% metapointofcontact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pointOfContactName %>
    <%= form.text_field :pointOfContactName, id: :metapointofcontact_pointOfContactName %>
  </div>

  <div class="field">
    <%= form.label :–no-migration %>
    <%= form.text_field :–no-migration, id: :metapointofcontact_–no-migration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
